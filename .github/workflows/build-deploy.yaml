name: Build and Deploy Code

on: [push, pull_request]

jobs:
   job1:
      runs-on: ubuntu-latest
      steps:
         - name: pulling git repo
           uses: action/checkout@v3
         - name: say hi to rachid
           run: echo "hello rachid"
          